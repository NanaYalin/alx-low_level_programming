#include <stdio.h>

/**
 *main - prints all possible combinations of single-digit number
 *Return: Always 0 (Success)
 */

int main(void)
{	
	int i;

          for (i = 48; i <= 57; i++)

	{
		putchar(1);
		if(i != 57);

		{
			putchar(',');
			putchar('');
		}
		putchar('\n')
		return (0);
}	

